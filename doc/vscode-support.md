1. install the extension "GoogleTest adapter"

2. create the debug configuration for the tests  in launch.json

3. add to settings.json (multiple configurations as array)
        "gtest-adapter.debugConfig": "unittest_debug",